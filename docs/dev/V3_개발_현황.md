# V3 프로그램 개발 현황

## 📊 프로젝트 개요
- **프로그램명:** 입고 정보 조회 V3 (inbound_status_v3.py)
- **목적:** OSIS 시스템에서 입고 데이터 자동 수집 및 CSV 저장
- **위치:** `C:\OSIS_AUTO\Inbound Status\`

---

## ✅ 완료된 기능 (2025-10-23)

### 1. 기본 구조
- ✅ 2단계 통신 최적화 (send1 → send2)
- ✅ 세션 쿠키 자동 관리
- ✅ except.csv 상품 필터링 (60개)
- ✅ XML 응답 파싱 (↑ 구분자 처리)

### 2. 데이터 저장
- ✅ 4개 CSV 파일 분리 저장
  - send1_Recdh (입고예정 헤더)
  - send1_Recdi (입고예정 상세) - 빈 데이터
  - send2_Recdh (입고내역 헤더)
  - send2_Recdi (입고내역 상세)
- ✅ 빈 컬럼 자동 필터링
- ✅ key 컬럼 포함 수정 완료

### 3. 디버그 기능
- ✅ XML 응답 저장 (debug_response_send1/2.txt)
- ✅ 백업 파일 자동 생성 (타임스탬프)

---

## 🔧 현재 진행 중

### 날짜 자동 계산 문제
**문제:**
- send1.txt에 하드코딩된 날짜: `20251021^20251022`
- `{QUERY_DATE}` 플레이스홀더 없음
- 날짜 자동 계산 함수는 있지만 작동 안 함

**해결 필요:**
1. send1.txt 날짜 부분을 `{QUERY_DATE}` 플레이스홀더로 변경
2. `generate_request_packet()` 함수 활성화 확인
3. date_offset 파라미터 검증

---

## 📋 대기 중인 작업

### 1. 데이터 분석
- [ ] 4개 CSV 파일 구조 분석
- [ ] send1 vs send2 Recdh 차이점 확인
- [ ] Recdi 데이터 존재 여부 검증

### 2. 조인 로직
- [ ] `merge_header_detail()` 함수 검토
- [ ] 조인 필요성 확인
- [ ] 최종 출력 포맷 결정

### 3. 추가 기능
- [ ] 날짜 범위 조회 기능
- [ ] 에러 핸들링 강화
- [ ] 로그 기능 추가

---

## 🎯 주요 함수

### 데이터 추출
```python
extract_table_data(xml_content, table_name)
# XML에서 Recdh/Recdi 테이블 추출
# 컬럼: key 포함 모든 컬럼
# 데이터: ↑ 구분자로 파싱
```

### 날짜 처리
```python
generate_request_packet(template_content, date_offset=0)
# date_offset: 0=오늘, 1=내일
# {QUERY_DATE} 플레이스홀더 치환
```

### 조인
```python
merge_header_detail(header_columns, header_records, 
                   detail_columns, detail_records)
# 입고문서번호로 헤더-상세 조인
# 현재 미사용 상태
```

---

## 📊 데이터 현황 (2025-10-23 20:51)

| 파일 | 컬럼 | 레코드 | 비고 |
|------|------|--------|------|
| send1_Recdh | 30 | 46 | 입고예정 헤더 |
| send1_Recdi | 0 | 0 | 빈 데이터 |
| send2_Recdh | 30 | 46 | 입고내역 헤더 |
| send2_Recdi | 66 | 306 | 입고내역 상세 |

---

## 🐛 알려진 이슈

### 1. 날짜 하드코딩 (긴급)
- **위치:** send1.txt 32~44번째 줄
- **내용:** `signIn^BW^20251021^20251022`
- **영향:** 오래된 데이터 조회, 빈 결과 가능
- **우선순위:** 높음

### 2. send1 Recdi 빈 데이터
- **현상:** send1 응답에 Recdi 데이터 없음 (0개)
- **원인:** 시스템 응답 특성 (정상)
- **해결:** 불필요 (send2에서 조회)

---

## 📝 개발 노트

### 2025-10-23 주요 변경사항
1. **key 컬럼 누락 수정**
   - extract_table_data() 함수에서 key 제외 로직 제거
   - 모든 컬럼 포함하도록 수정

2. **4개 CSV 분리 저장**
   - 기존 2개 → 4개 파일로 변경
   - send1/send2 각각 Recdh/Recdi 추출

3. **다음 대화 예정**
   - send1.txt 날짜 플레이스홀더 추가
   - 4개 CSV 분석
   - 조인 로직 활성화 검토

---

## 🔗 관련 파일

### 프로그램
- `inbound_status_v3.py` - 메인 프로그램
- `login.txt` - 로그인 패킷
- `send1.txt` - 입고예정 조회 패킷
- `send2.txt` - 입고내역 조회 패킷
- `except.csv` - 제외 상품 목록

### 출력
- `send1_Recdh_YYYYMMDD.csv`
- `send1_Recdi_YYYYMMDD.csv`
- `send2_Recdh_YYYYMMDD.csv`
- `send2_Recdi_YYYYMMDD.csv`
- `debug_response_send1.txt`
- `debug_response_send2.txt`

---

**마지막 업데이트:** 2025-10-23 20:52
**다음 작업:** 날짜 플레이스홀더 적용 및 데이터 분석
